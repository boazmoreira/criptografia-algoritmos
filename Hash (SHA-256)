import hashlib

mensagem = "mensagem a ser protegida"
hash_obj = hashlib.sha256(mensagem.encode())
hash_final = hash_obj.hexdigest()

print("Mensagem:", mensagem)
print("Hash SHA-256:", hash_final)
